import { css } from '@emotion/core';

export const innerDiv = css({ height: '100%' });
export const relativeCell = css({ position: 'relative' });
export const relativeChildren = css({ position: 'absolute', top: '0' });
