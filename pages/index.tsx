import { FC } from 'react';
import Link from 'next/link';
import Form from '../components/form';

const Index: FC = () => {
  return (
    <div>
      <Form />
    </div>
  );
};

export default Index;
